# How to hook Pi to your Windows PC (Direct Ethernet)

Requirements:
- Raspberry Pi 4 + microSD (Raspberry Pi OS)
- Pi 4 power supply (USB-C 5V/3A)
- Ethernet cable (PC <-> Pi)

# Windows settings
1. Settings → Network & Internet → Mobile Hotspot
   Mobile Hotspot: ON
2. Settings → Network & Internet → Advanced network settings → Change adapter options
   Right-click main adapter → Properties → Sharing
   Allow other network users to connect
   Choose the Ethernet adapter connected to the Pi

# Find Pi IP from Windows
arp -a

# SSH into the Pi
ssh <username>@<pi-ip>

# Test remote command
ssh <username>@<pi-ip> "echo OK && uname -a"

# Inline Python test on Pi
python3 - << 'PY'
print("Hello")
PY

ping raspberrypi.local
arp -a
ssh kwoc393@raspberrypi.local

echo "deb https://packages.cloud.google.com/apt coral-edgetpu-stable main" | sudo tee /etc/apt/sources.list.d/coral-edgetpu.list
curl -LO 'https://packages.cloud.google.com/apt/doc/apt-key.gpg'
sudo mv apt-key.gpg /etc/apt/keyrings/
sudo vi /etc/apt/sources.list.d/coral-edgetpulist

# Windows PowerShell
sftp kwoc393@192.168.137.23
lcd Downloads
put apt-key.gpg

ping -c 3 8.8.8.8
ifconfig
sudo raspi-config
sudo nmcli connection up eduroam
sudo nmcli connection up eduroam --ask
sudo nmcli connection edit eduroam

less /etc/apt/keyrings/apt-key.gpg
mv /etc/apt/keyrings/{apt-key.gpg,coral.gpg}
cat /etc/apt/sources.list.d/
cat /etc/apt/sources.list.d/coral-edgetpu.list
cat /etc/apt/sources.list.d/coral-edgetpulist
sudo rm /etc/apt/sources.list.d/coral-edgetpu.list
sudo mv /etc/apt/sources.list.d/{coral-edgetpulist,coral-edgetpu.list}
sudo mv /etc/apt/keyrings/{apt-key.gpg,coral.gpg}
sudo apt-get update
hostname
hostname -I
ip addr show eth0
systemctl status avahi-daemon
dmesg | egrep -i 'eth0|lan78|link is (up|down)'

# Prereq
uname -m
ping -c 3 8.8.8.8

# Miniforge
cd ~
wget https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-Linux-aarch64.sh
bash Miniforge3-Linux-aarch64.sh
exec $SHELL

# Environment
cd ~
nano tflite-py39.yml

# tflite-py39.yml
name: tflite-py39
channels:
  - conda-forge
dependencies:
  - python=3.9
  - pip
  - numpy
  - pillow
  - scipy
  - opencv
  - protobuf<4
  - setuptools
  - wheel

conda env create -f tflite-py39.yml
conda activate tflite-py39

# Coral install
pip install --extra-index-url https://google-coral.github.io/py-repo/ \
    tflite-runtime==2.5.0.post1 pycoral
